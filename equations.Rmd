---
title: "equations"
output: html_document
date: "`r Sys.Date()`"
---

$$
if 
$$

if before peak $prevalence \space ratio = (prev_n - prev_{n+1})/(prev_{max} - prev_n)$
if after peak  $prevalence \space ratio = 1-((prev_{n+1} - prev_n)/(prev_{max} - prev_n))$